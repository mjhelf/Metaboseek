app <- ShinyDriver$new("../")
app$snapshotInit("mytest")

# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
app$setInputs(runcode_expr = "print")
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
app$setInputs(runcode_expr = "print(")
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
app$setInputs(runcode_expr = "print(\"")
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
app$setInputs(runcode_expr = "print(\"test")
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
app$setInputs(runcode_expr = "print(\"test\"")
# Input 'keyd' was set, but doesn't have an input binding.
# Input 'keyd' was set, but doesn't have an input binding.
app$setInputs(runcode_expr = "print(\"test\")")
app$setInputs(runcode_run = "click")
app$snapshot()
